# .nixpacks
# Define la versión de Node.js a usar.
[environment]
NODE_VERSION = "22" # Aquí usamos la versión principal "22"

# Define los comandos de construcción para tu backend
[phases.build]
commands = ["npm install"]

# Define el comando de inicio para tu backend
[start]
cmd = "npm start"